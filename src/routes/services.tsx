import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";
import { PageIntro, SiteLayout } from "@/components/site";
import { services } from "@/lib/site-data";
export const Route=createFileRoute("/services")({head:()=>({meta:[{title:"Event Planning Services — Dream Corner"},{name:"description",content:"Explore themed birthdays, traditional celebrations, weddings, corporate events and event add-ons."},{property:"og:title",content:"Event Planning Services — Dream Corner"},{property:"og:description",content:"Thoughtful event planning, styling and execution for every kind of celebration."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}],links:[{rel:"canonical",href:"/services"}]}),component:Services});
function Services(){return <SiteLayout><PageIntro eyebrow="What we create" title="Celebrations, thoughtfully created." copy="Every service begins with your vision and is shaped around your occasion, priorities and budget."/><section className="content-section"><div className="site-container">{services.map(s=><article id={s.slug} key={s.slug} className="service-detail"><img src={s.image} alt={`Temporary preview for ${s.title}`} loading="lazy"/><div><span className="eyebrow text-gold">{s.number} · Service</span><h2 className="mt-4">{s.title}</h2><p className="mt-5">{s.summary}</p><ul>{s.types.map(t=><li key={t}>{t}</li>)}</ul><Link to="/contact" search={{eventType:s.title}} className="button-primary mt-7">Enquire about this service <ArrowRight size={16}/></Link></div></article>)}</div></section></SiteLayout>}